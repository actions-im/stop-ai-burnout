Record an explicit out-of-scope request for the current session.

Run:

```bash
power out-of-scope --reason "$ARGUMENTS"
```

If the session enters `Converge`, explain that only these actions are now allowed:
- complete an approved task
- drop an approved task
- rewrite approved scope downward
- park new ideas
- shut down
