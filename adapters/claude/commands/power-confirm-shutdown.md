Confirm shutdown and return the session to `Idle`.

Run:

```bash
power confirm-shutdown --reason "Closed cleanly from Claude"
```

After the command succeeds, state only that the governed session is closed.
