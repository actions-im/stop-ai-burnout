Start a governed Power Governor session for the current repo.

Run the local CLI in the repo root:

```bash
power start --mission "$ARGUMENTS"
```

Before running the command, ask for exactly two approved tasks if they were not provided. After the command succeeds, show the resulting mission, task cap, mode, and timebox. Do not begin implementation work in this command.
