Park a new idea without changing scope.

Run:

```bash
power park --idea "$ARGUMENTS" --reason "Parked from Claude command"
```

Confirm that the idea was parked and remind the user that approved work did not change.
