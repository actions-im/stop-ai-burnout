Search the active parking lot for ideas related to the given query.

Run:

```bash
power parking-search --query "$ARGUMENTS" --json
```

Summarize all matches in score order with:
- entry id
- idea
- reason
- overlap score details

If there are no matches, say so plainly.
