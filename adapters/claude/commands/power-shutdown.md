Run the governed shutdown flow.

Collect only:
- completed approved items
- unfinished approved items
- open factual questions
- parking references

Then run the local CLI in the repo root using repeated flags as needed:

```bash
power shutdown
```

Return the shutdown summary exactly as produced. Do not add next steps, roadmap items, or optimization ideas.
