Check the current Power Governor session state for the current repo.

Run:

```bash
power check --json
```

Summarize only:
- current mode
- mission
- approved tasks
- time remaining if visible
- drift count
- override count
